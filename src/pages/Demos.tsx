import { useState } from "react"
import { Link } from "react-router-dom"
import Meta from "../components/Meta"
import { agents } from "../data/site"
export default function Demos() { const [tab, setTab] = useState("commerce"); const [cart, setCart] = useState(0); const [pet, setPet] = useState("Max"); return <><Meta title="Interactive Business Demos | DM Digital Solutions" description="Try fictional interactive examples of e-commerce, booking, veterinary management, cafe ordering, dashboards, and AI agents." /><section className="page-intro"><div className="container narrow"><p className="eyebrow">Demo center</p><h1>Don't just take our word for it. Try what we build.</h1><p className="lead">Every example below uses fictional data. It is a product conversation starter, not a live customer system.</p></div></section><section className="section"><div className="container"><div className="demo-tabs" role="tablist">{[["commerce", "E-commerce"], ["booking", "Dental booking"], ["veterinary", "Veterinary"], ["cafe", "Cafe ordering"], ["dashboard", "Business dashboard"], ["ai", "AI agent"]].map(([id, label]) => <button key={id} type="button" role="tab" aria-selected={tab === id} className={tab === id ? "is-active" : ""} onClick={() => setTab(id)}>{label}</button>)}</div><div className="demo-stage"><p className="demo-label">DEMO DATA</p>{tab === "commerce" && <div className="demo-content"><h2>Owned commerce</h2><p>Product catalog → cart → checkout preview → order confirmation.</p><div className="demo-products">{["Field Notes", "Studio Tote", "Desk Lamp"].map((item, index) => <button type="button" key={item} onClick={() => setCart(cart + 1)}><strong>{item}</strong><span>PHP {[499, 799, 1299][index].toLocaleString()}</span><small>Add to cart</small></button>)}</div><div className="demo-result">{cart} item{cart === 1 ? "" : "s"} in cart <Link to="/contact">Build this flow →</Link></div></div>}{tab === "booking" && <div className="demo-content"><h2>Dental appointment</h2><div className="booking-flow"><span className="is-active">1 Service</span><span>2 Date</span><span>3 Details</span><span>4 Confirm</span></div><p>Dental Cleaning · Dr. Santos · August 20 · 2:00 PM</p><Link className="button" to="/contact?project=Booking%20system">Discuss a booking system</Link></div>}{tab === "veterinary" && <div className="demo-content"><h2>Veterinary management</h2><div className="pet-switcher">{["Max", "Mochi", "Bruno"].map((name) => <button type="button" className={pet === name ? "is-active" : ""} key={name} onClick={() => setPet(name)}>{name}</button>)}</div><p>{pet} · fictional pet profile</p><dl className="demo-dl"><div><dt>Next appointment</dt><dd>August 22</dd></div><div><dt>Vaccination</dt><dd>Due September 10</dd></div><div><dt>Visit history</dt><dd>3 records</dd></div></dl></div>}{tab === "cafe" && <div className="demo-content"><h2>Cafe pickup order</h2><p>Latte · Americano · Croissant</p><button className="button" type="button" onClick={() => setCart(cart + 1)}>Add item · {cart} selected</button></div>}{tab === "dashboard" && <div className="demo-content"><h2>Business operations dashboard</h2><div className="metric-grid">{[["Orders", "24"], ["Customers", "86"], ["Appointments", "12"], ["Tasks", "9"]].map(([label, value]) => <div key={label}><span>{label}</span><strong>{value}</strong></div>)}</div></div>}{tab === "ai" && <div className="demo-content"><h2>Configurable AI agents</h2><div className="agent-grid">{agents.map((agent) => <article key={agent.id}><strong>{agent.name}</strong><p>{agent.description}</p><Link to={`/ai?agent=${agent.id}`}>Open agent →</Link></article>)}</div></div>}</div></div></section></> }
