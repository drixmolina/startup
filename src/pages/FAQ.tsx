import { useState } from "react"
import Meta from "../components/Meta"
import { faqs } from "../data/site"
export default function FAQ() { const [open, setOpen] = useState(0); return <><Meta title="FAQ | DM Digital Solutions" description="Answers to common questions about websites, e-commerce, booking systems, custom software, AI, hosting, and maintenance." /><section className="page-intro"><div className="container narrow"><p className="eyebrow">FAQ</p><h1>Good questions make better systems.</h1><p className="lead">A few practical answers about scope, process, and the kinds of digital work DM Digital Solutions can support.</p></div></section><section className="section"><div className="container narrow faq-list">{faqs.map(([question, answer], index) => <div className="faq-item" key={question}><button type="button" aria-expanded={open === index} onClick={() => setOpen(open === index ? -1 : index)}><span>{question}</span><strong>{open === index ? "−" : "+"}</strong></button>{open === index && <p>{answer}</p>}</div>)}</div></section></> }